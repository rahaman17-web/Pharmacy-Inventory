# Copy this file to .env and fill in your values
# Never commit .env to version control

PORT=4002

# Local development (host machine)
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost:5432/pharmacy

# Docker — replace localhost with db (postgres service name)
# DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@db:5432/pharmacy

JWT_SECRET=change-this-to-a-long-random-string
JWT_EXPIRES_IN=7d

ADMIN_USER=admin
ADMIN_PASSWORD=change-this-password
